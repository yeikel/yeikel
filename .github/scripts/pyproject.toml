[project]
name = "yeikel-profile-updater"
version = "0.1.0"
description = "Update dynamic sections of the GitHub profile README"
requires-python = ">=3.11"
dependencies = [
    "PyGithub==2.9.1",
]

[tool.uv]
package = false
required-version = "==0.12.1"
